# Reference Fixture

This fixture is the curated active scenario data under `data/tasks/`, `data/contracts/`, `data/policies/`, and `data/operations/employee_operations.sqlite`.

Runtime code must discover candidate tasks from raw status and date fields. Tests may assert known reference fixture expectations, but the model must not receive those expectations.
