# Blind Fixtures

Blind fixtures should be created later with renamed employees, changed task IDs, shifted dates, varied candidate counts, and at least one new causal pattern.

The runtime must not contain special handling for the reference employee names, task IDs, or case count.
