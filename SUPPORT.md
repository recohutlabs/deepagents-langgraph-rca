# Support

This is a demo repository for architecture validation. It is provided as-is.

Use the docs in `docs/` and the commands in `README.md` first. For production usage, replace synthetic evidence, use persistent checkpoint storage, add authentication, add tenant isolation, and run a formal evaluation program.
